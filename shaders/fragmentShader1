#version 330 core

out vec4 FragColor;

in vec3 Position;
in vec3 Normal;
in vec2 TexCoord;
flat in ivec4 BoneIDs;
in vec4 Weights;

uniform sampler2D texture_diffuse1;

void main(){
	FragColor = texture(texture_diffuse1, TexCoord);
}